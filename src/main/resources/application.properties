spring.application.name=cambio-service

server.port=8100
ome do servi�o
spring.application.name=cambio-service

spring.datasource.url=jdbc:postgresql://localhost:5432/cambio_db_b
spring.datasource.username=seu_usuario_aqui
spring.datasource.password=sua_senha_aqui
spring.datasource.driver-class-name=org.postgresql.Driver


spring.jpa.hibernate.ddl-auto=none  # N�o recria o banco de dados, j� que vamos usar as migra��es Flyway
spring.jpa.show-sql=true  # Exibe as queries SQL no console para fins de depura��o
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.flyway.enabled=true
spring.flyway.locations=classpath:db_migration  # Diret�rio onde os arquivos de migra��o est�o localizados

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
